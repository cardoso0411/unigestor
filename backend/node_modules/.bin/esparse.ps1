#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:backend/node_modules/.bin/esparse.ps1
    $input | & "$basedir/node$exe"  "$basedir/../esprima/bin/esparse.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../esprima/bin/esparse.js" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../js-yaml/bin/js-yaml.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../js-yaml/bin/js-yaml.js" $args
>>>>>>>> 0a741ea58f249dc3063a429f349de6d52c6cd27d:backend/node_modules/.bin/js-yaml.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:backend/node_modules/.bin/esparse.ps1
    $input | & "node$exe"  "$basedir/../esprima/bin/esparse.js" $args
  } else {
    & "node$exe"  "$basedir/../esprima/bin/esparse.js" $args
========
    $input | & "node$exe"  "$basedir/../js-yaml/bin/js-yaml.js" $args
  } else {
    & "node$exe"  "$basedir/../js-yaml/bin/js-yaml.js" $args
>>>>>>>> 0a741ea58f249dc3063a429f349de6d52c6cd27d:backend/node_modules/.bin/js-yaml.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
